import React, { Component } from 'react';
import { Button } from 'react-native';

export default class Bluetooth extends Component {
    render() {
        return (
            <Button title="Bluetooth Setup" />
        );
    }
}